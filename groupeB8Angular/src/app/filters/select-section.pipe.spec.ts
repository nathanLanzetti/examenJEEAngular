import { SelectSectionPipe } from './select-section.pipe';

describe('SelectSectionPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectSectionPipe();
    expect(pipe).toBeTruthy();
  });
});
