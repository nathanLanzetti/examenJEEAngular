import { SelectBlocPipe } from './select-bloc.pipe';

describe('SelectBlocPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectBlocPipe();
    expect(pipe).toBeTruthy();
  });
});
